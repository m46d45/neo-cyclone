# Optional: enables cloud AI drafting of CYCLONE models (xAI / Grok API).
# Without this key the app still works using the local prompt → model builder.
XAI_API_KEY=

# Optional auth (leave unset for open studio with PGLite fallback)
# Optional: Neon Postgres. If set, the worldwide usage counter is stored
# in your own database. If unset, the live studio still keeps a durable
# public tally (anonymous draw + simulate counts).
# DATABASE_URL=
# BETTER_AUTH_SECRET=
# BETTER_AUTH_URL=https://your-app.vercel.app
